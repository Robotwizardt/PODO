<Window x:Class="WitchDrawer.App.FileDialogAccess.FileDialogAccessWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        x:ClassModifier="internal"
        Title="文件对话框访问窗"
        Width="320"
        MinWidth="240"
        MaxWidth="520"
        MinHeight="240"
        ShowInTaskbar="False"
        ShowActivated="False"
        WindowStyle="None"
        ResizeMode="CanResizeWithGrip"
        Background="{DynamicResource AppBackgroundBrush}"
        Foreground="{DynamicResource TextPrimaryBrush}"
        PreviewKeyDown="OnPreviewKeyDown">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
        <Style x:Key="HeaderIconButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="30" />
            <Setter Property="Height" Value="30" />
            <Setter Property="Margin" Value="2,0" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="{DynamicResource TextMutedBrush}" />
            <Setter Property="FontFamily" Value="Segoe MDL2 Assets" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="Cursor" Value="Hand" />
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource HoverBrush}" />
                    <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}" />
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="AccessItemButtonStyle" TargetType="Button">
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="8,7" />
            <Setter Property="Margin" Value="0,1" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="ToolTip" Value="{Binding StoragePath}" />
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource HoverBrush}" />
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Opacity" Value="0.48" />
                    <Setter Property="Cursor" Value="Arrow" />
                </Trigger>
            </Style.Triggers>
        </Style>
        <DataTemplate x:Key="AccessEntryTemplate">
            <Button Style="{StaticResource AccessItemButtonStyle}"
                    Command="{Binding NavigateCommand}"
                    AutomationProperties.Name="{Binding Name}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="30" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0"
                               Text="{Binding TypeGlyph}"
                               FontFamily="Segoe MDL2 Assets"
                               FontSize="16"
                               Foreground="{DynamicResource AccentBrush}"
                               VerticalAlignment="Center" />
                    <StackPanel Grid.Column="1">
                        <TextBlock Text="{Binding Name}"
                                   FontSize="12.5"
                                   FontWeight="SemiBold"
                                   TextTrimming="CharacterEllipsis" />
                        <TextBlock Text="{Binding TypeLabel}"
                                   FontSize="10.5"
                                   Foreground="{DynamicResource TextMutedBrush}"
                                   TextTrimming="CharacterEllipsis" />
                        <TextBlock Text="{Binding StatusText}"
                                   FontSize="10.5"
                                   Foreground="{DynamicResource DangerBrush}" />
                    </StackPanel>
                </Grid>
            </Button>
        </DataTemplate>
    </Window.Resources>

    <Border BorderBrush="{DynamicResource BorderBrushSoft}"
            BorderThickness="1"
            Background="{DynamicResource AppBackgroundBrush}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="44" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="12,0,6,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock VerticalAlignment="Center"
                           Text="收纳盒"
                           FontSize="14"
                           FontWeight="SemiBold" />
                <Button Grid.Column="1"
                        Style="{StaticResource HeaderIconButtonStyle}"
                        Content="&#xE72C;"
                        Command="{Binding RefreshCommand}"
                        ToolTip="刷新"
                        AutomationProperties.Name="刷新收纳盒" />
                <Button x:Name="MoreButton"
                        Grid.Column="2"
                        Style="{StaticResource HeaderIconButtonStyle}"
                        Content="&#xE712;"
                        ToolTip="更多"
                        AutomationProperties.Name="更多选项"
                        Click="OnMoreButtonClick">
                    <Button.ContextMenu>
                        <ContextMenu Placement="Bottom">
                            <MenuItem Header="对此应用不再显示"
                                      Command="{Binding PlacementTarget.DataContext.BlacklistCommand, RelativeSource={RelativeSource AncestorType=ContextMenu}}" />
                        </ContextMenu>
                    </Button.ContextMenu>
                </Button>
                <Button Grid.Column="3"
                        Style="{StaticResource HeaderIconButtonStyle}"
                        Content="&#xE711;"
                        Command="{Binding CloseCommand}"
                        ToolTip="关闭当前访问窗"
                        AutomationProperties.Name="关闭当前访问窗" />
            </Grid>

            <Grid Grid.Row="1" Margin="10,0,10,8">
                <TextBlock Text="&#xE721;"
                           FontFamily="Segoe MDL2 Assets"
                           Foreground="{DynamicResource TextMutedBrush}"
                           VerticalAlignment="Center"
                           Margin="9,0,0,0"
                           Panel.ZIndex="1"
                           IsHitTestVisible="False" />
                <TextBox x:Name="SearchBox"
                         Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                         Height="34"
                         Padding="30,7,8,6"
                         VerticalContentAlignment="Center"
                         BorderBrush="{DynamicResource BorderBrushSoft}"
                         Background="{DynamicResource PanelBrush}"
                         AutomationProperties.Name="搜索收纳盒" />
            </Grid>

            <Border Grid.Row="2"
                    Margin="10,0,10,8"
                    Padding="8,6"
                    Background="{DynamicResource DangerSoftBrush}"
                    Visibility="{Binding HasError, Converter={StaticResource BooleanToVisibilityConverter}}">
                <TextBlock Text="{Binding ErrorText}"
                           Foreground="{DynamicResource DangerBrush}"
                           FontSize="11"
                           TextWrapping="Wrap" />
            </Border>

            <ScrollViewer x:Name="AccessScrollViewer"
                          Grid.Row="3"
                          VerticalScrollBarVisibility="Auto"
                          HorizontalScrollBarVisibility="Disabled"
                          PreviewMouseWheel="OnAccessScrollViewerPreviewMouseWheel">
                <StackPanel Margin="10,0,10,10">
                    <StackPanel Visibility="{Binding HasRecentEntries, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <TextBlock Text="最近使用"
                                   Margin="6,4,6,3"
                                   FontSize="10.5"
                                   FontWeight="SemiBold"
                                   Foreground="{DynamicResource TextMutedBrush}" />
                        <ItemsControl ItemsSource="{Binding RecentEntries}"
                                      ItemTemplate="{StaticResource AccessEntryTemplate}" />
                        <Separator Margin="4,7" />
                    </StackPanel>

                    <TextBlock Text="全部收纳盒"
                               Margin="6,2,6,3"
                               FontSize="10.5"
                               FontWeight="SemiBold"
                               Foreground="{DynamicResource TextMutedBrush}" />
                    <ListBox x:Name="AccessList"
                             ItemsSource="{Binding Entries}"
                             ItemTemplate="{StaticResource AccessEntryTemplate}"
                             Background="Transparent"
                             BorderThickness="0"
                             ScrollViewer.CanContentScroll="True"
                             VirtualizingPanel.IsVirtualizing="True"
                             VirtualizingPanel.VirtualizationMode="Recycling"
                             AutomationProperties.Name="可访问的文件收纳盒">
                        <ListBox.ItemsPanel>
                            <ItemsPanelTemplate>
                                <VirtualizingStackPanel />
                            </ItemsPanelTemplate>
                        </ListBox.ItemsPanel>
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem">
                                <Setter Property="Padding" Value="0" />
                                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                <Setter Property="Background" Value="Transparent" />
                                <Setter Property="BorderThickness" Value="0" />
                            </Style>
                        </ListBox.ItemContainerStyle>
                    </ListBox>
                    <TextBlock Text="没有匹配的收纳盒"
                               Margin="8,18"
                               HorizontalAlignment="Center"
                               Foreground="{DynamicResource TextMutedBrush}">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Visibility" Value="Collapsed" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding HasEntries}" Value="False">
                                        <Setter Property="Visibility" Value="Visible" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Border>
</Window>
